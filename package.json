{
  "name": "jitsi-loadtest-pupeter",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "git tag -a v${npm_package_version} -m \"Release v${npm_package_version}\" && git push origin --follow-tags",
    "release:major": "npm version major && git push origin --follow-tags",
    "release:minor": "npm version minor && git push origin --follow-tags",
    "release:patch": "npm version patch && git push origin --follow-tags"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.3",
    "jsonwebtoken": "^9.0.2",
    "puppeteer": "^24.30.0",
    "puppeteer-cluster": "^0.25.0",
    "uuid": "^13.0.0"
  }
}